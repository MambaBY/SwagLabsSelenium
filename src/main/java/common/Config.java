package common;

public class Config {
    /**
     * Clear cookie after each iteration
     * if true - clear cookies
     */
    public static final Boolean CLEAR_COOKIES_AND_STORAGE = true;

    /**
     * To keep browser open after suit
     * if true - browser close
     */
    public static final Boolean HOLD_BROWSER_OPENED = true;
}
